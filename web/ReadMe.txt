We used JSP Java Servlet Page as the programming language for developing the application.

The software which we run the application are as follows:

1. NetBean, Wamp server and JDK.

2. MySQL-Java-Database-Connector is needed for the application to be connected to the database.
create and Import the Database in to the wamp server
all the web application content are saved in Web file
